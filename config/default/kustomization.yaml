namespace: tep-operator-system
namePrefix: tep-operator-

resources:
- ../crd
- ../rbac
- ../manager
- metrics_service.yaml

patches:
- path: manager_metrics_patch.yaml
  target:
    kind: Deployment

# ── Desabilitado (descomentar quando precisar) ──────────────────────────────
# - ../webhook          # Webhooks de validacao/defaulting
# - ../certmanager      # TLS com cert-manager
# - ../prometheus       # ServiceMonitor pro Prometheus
# - ../network-policy   # NetworkPolicy (removido do repo — recriar se necessario)
